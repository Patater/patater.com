PKG nginx py37-certbot
OVERLAY etc
OVERLAY usr
SYSRC nginx_enable=YES
SERVICE nginx restart
RDR tcp 80 80
PRE openssl dhparam -out /usr/local/etc/nginx/dhparams.pem 2048
PRE certbot certonly --webroot -w /usr/local/www/patater.com/htdocs -d patater.net -d www.patater.net -m certbot@patater.net --agree-tos
